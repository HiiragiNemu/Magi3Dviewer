# Stage 608 current-JP Material extraction

This research branch uses the existing bounded current-JP resource protocol only to fetch `battle/stage/bg_3d_608_00_00_001` into a temporary runner directory. It persists Material/Texture typetree evidence only; raw AssetBundle bytes are not committed.

The immediate purpose is to verify `bg3d608_00_blue_Fish` texture bindings, `_MULTI_UV_SCROLL` keyword/property state, Unity texture ST, and other red/blue stage material settings before correcting the deployed Web reconstruction.
